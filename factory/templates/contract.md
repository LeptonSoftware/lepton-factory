# Execution Contract — WO-XXXX

<!-- Compiled by compile-contract (Gate 0) from Requirement + Blueprints + Work Order
BEFORE planning. Purpose: make the obligations explicit and surface specification
holes early. Everything here is derived from authoritative records — cite the source
ID for every entry. If compilation cannot fill a section, that is a finding, not a
blank to skip: record it under Known Unknowns or Human Decisions. -->

## Acceptance criteria (verbatim)

<!-- Copy each AC exactly as written, with ID. This is the only sanctioned prose copy —
reviewers check it against the source. -->

## Invariants

<!-- System invariants that must hold after this change (from blueprints: idempotency,
ordering, transaction boundaries, consistency, failure semantics, compatibility). -->

## Interfaces touched

<!-- Public APIs, events, schemas, storage shapes this WO may change — and the
compatibility rule for each (breaking allowed? versioned? additive only?). -->

## Disallowed changes

<!-- Paths, behaviors, and contracts this WO must NOT alter (from Out of Scope +
blueprint ownership). -->

## Verification contract

<!-- One row per AC. This is what Gate C audits against.
| COV ID | AC | Automated test | Runtime action | Expected evidence | Negative case |
|---|---|---|---|---|---|
-->

## Known unknowns

<!-- Questions the records do not answer, each with: how it will be resolved
(investigate during context / spike / escalate). -->

## Human decisions required (HITL)

<!-- From the WO's decision classification plus anything discovered during compilation.
Execution stops on these; they are never resolved by iteration. -->
